export LC_ALL=C

# pg
pgbasepath=~/.postgres
if [[ ! -d $pgbasepath ]]; then
  mkdir -pv $pgbasepath
fi

# postgresql home path
export PGDATA=$pgbasepath

# ruby Encoding.default_external
export RUBYOPT=-EUTF-8
